const add = (a, b)=>{
    return a + b;
}

const sub = (a, b)=>{
    return a - b;
}

const name = "kamlesh";

// module.exports.name = name;
// module.exports.add = add;
// module.exports.sub = sub;

module.exports = {sub, name, add}